# Copy to .env and set values. Use __ for nested keys (e.g. Logging__LogLevel__Default).
# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5042;https://localhost:7027

# Logging (optional; defaults apply if omitted)
# Logging__LogLevel__Default=Information
# Logging__LogLevel__Microsoft_AspNetCore=Warning

# Application (for later milestones)
# MySqlServerConnectionString=Server=.;Database=UserManagement;Trusted_Connection=True;TrustServerCertificate=True;
# FeatureFlags__EnableOtp=true
# AWS__Region=us-east-1
# SES__FromEmail=noreply@example.com
# Cognito__UserPoolId=
# Cognito__ClientId=
